      - name: 📝 إنشاء ملف المشروع
        run: |
          echo "📝 إنشاء مشروع أندرويد..."
          
          # قراءة البيانات
          APP_IDEA=$(cat .forge/app_idea.txt)
          APP_NAME=$(cat .forge/app_name.txt)
          PACKAGE_NAME=$(cat .forge/package_name.txt)
          BUILD_ID="${{ needs.prepare-build.outputs.build_id }}"
          
          echo "🔧 بناء تطبيق: $APP_NAME"
          echo "🏷️ الحزمة: $PACKAGE_NAME"
          echo "💡 الفكرة: $APP_IDEA"
          
          # إنشاء هيكل المشروع المبسط
          mkdir -p app/src/main/java/com/example/app
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values
          
          # 1. إنشاء AndroidManifest.xml - باستخدام echo مباشرة
          echo '<?xml version="1.0" encoding="utf-8"?>' > app/src/main/AndroidManifest.xml
          echo '<manifest xmlns:android="http://schemas.android.com/apk/res/android">' >> app/src/main/AndroidManifest.xml
          echo '    ' >> app/src/main/AndroidManifest.xml
          echo '    <uses-permission android:name="android.permission.INTERNET" />' >> app/src/main/AndroidManifest.xml
          echo '    ' >> app/src/main/AndroidManifest.xml
          echo '    <application' >> app/src/main/AndroidManifest.xml
          echo '        android:allowBackup="true"' >> app/src/main/AndroidManifest.xml
          echo "        android:icon=\"@mipmap/ic_launcher\"" >> app/src/main/AndroidManifest.xml
          echo "        android:label=\"$APP_NAME\"" >> app/src/main/AndroidManifest.xml
          echo '        android:theme="@style/Theme.AppCompat.Light">' >> app/src/main/AndroidManifest.xml
          echo '        ' >> app/src/main/AndroidManifest.xml
          echo '        <activity' >> app/src/main/AndroidManifest.xml
          echo '            android:name=".MainActivity"' >> app/src/main/AndroidManifest.xml
          echo '            android:exported="true">' >> app/src/main/AndroidManifest.xml
          echo '            <intent-filter>' >> app/src/main/AndroidManifest.xml
          echo '                <action android:name="android.intent.action.MAIN" />' >> app/src/main/AndroidManifest.xml
          echo '                <category android:name="android.intent.category.LAUNCHER" />' >> app/src/main/AndroidManifest.xml
          echo '            </intent-filter>' >> app/src/main/AndroidManifest.xml
          echo '        </activity>' >> app/src/main/AndroidManifest.xml
          echo '        ' >> app/src/main/AndroidManifest.xml
          echo '    </application>' >> app/src/main/AndroidManifest.xml
          echo '</manifest>' >> app/src/main/AndroidManifest.xml
          
          echo "✅ تم إنشاء AndroidManifest.xml"
          
          # 2. إنشاء MainActivity.kt - باستخدام cat مع اقتباس مزدوج
          cat > app/src/main/java/com/example/app/MainActivity.kt << "EOF"
package com.example.app

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import android.widget.TextView

class MainActivity : AppCompatActivity() {
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        
        // إنشاء واجهة بسيطة
        val textView = TextView(this)
        textView.text = \"\"\"
            🚀 $APP_NAME
            
            💡 الفكرة:
            $APP_IDEA
            
            📱 تم البناء بواسطة:
            Ultimate Forge
            
            ⏰ التاريخ: $(date)
            🆔 المعرف: $BUILD_ID
            
            شكراً لاستخدامك Ultimate Forge!
        \"\"\".trimIndent()
        
        textView.setTextSize(16f)
        textView.setPadding(32, 32, 32, 32)
        textView.setTextColor(0xFF000000.toInt())
        
        setContentView(textView)
    }
}
EOF
          
          echo "✅ تم إنشاء MainActivity.kt"
          
          # 3. إنشاء ملفات build.gradle
          cat > build.gradle << 'EOF'
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:7.4.2"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
EOF
          
          cat > app/build.gradle << "EOF"
plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.example.app'
    compileSdk 34
    
    defaultConfig {
        applicationId "$PACKAGE_NAME"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
    
    buildTypes {
        release {
            minifyEnabled false
        }
        debug {
            debuggable true
        }
    }
    
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    
    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
}
EOF
          
          # 4. إنشاء الملفات الأخرى
          echo 'include ":app"' > settings.gradle
          
          cat > gradle.properties << 'EOF'
org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
EOF
          
          echo "✅ تم إنشاء مشروع أندرويد كامل"
