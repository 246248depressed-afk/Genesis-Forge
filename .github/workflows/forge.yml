# File: forge.yml (The PURE version)
name: The Forge (v44.0 - Unified Protocol)

on:
  workflow_dispatch:
    inputs:
      # We ONLY expect the 'idea'
      idea:
        description: 'The Android App Idea'
        required: true
        default: 'A simple "Hello World" app'

permissions:
  contents: write
  actions: write

jobs:
  build_process:
    runs-on: macos-latest
    steps:
      # ... (The rest of the code is the same as v43.0) ...
      # All steps from "Step 1: Checkout" to "Step 10: Upload Artifact"
      # remain exactly the same. The only change is in the 'on:' block.
      - name: Step 1: Checkout
        uses: actions/checkout@v4
      # ... and so on
